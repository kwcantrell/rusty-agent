# Generated Wrangler build artifacts — dev bundles, middleware loaders,
# bundled worker.js. These produced the dev_*/bundle_*/middleware_* noise
# communities in the graph (generated output, not authored architecture).
.wrangler/

# SDD task ledger — per-task process briefs/reports. Useful as a work log,
# but they flood the graph with task-N nodes that crowd out the real
# architecture. Excluded from the knowledge map.
.superpowers/sdd/

docs/superpowers
.claude/
.agents

# Dependency / build output trees — vendored deps and compiler artifacts, not
# authored architecture. These dwarf the real source if ingested.
node_modules/
agent/target/
src-tauri/target/
src-tauri/gen/

# Lock files — machine-generated dependency pins. Pure noise in the graph.
Cargo.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Local worktrees + tool scratch
.worktrees/
.playwright-mcp/
.superpowers/

# Graphify's own output (avoid ingesting the graph into itself)
graphify-out/

# OS / editor junk
.DS_Store
.idea/
.vscode/
